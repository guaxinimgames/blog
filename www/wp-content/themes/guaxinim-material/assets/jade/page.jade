extends index.jade
block main
	- while ( have_posts() ) : the_post();
		- get_template_part( 'content', 'page' );
		- if ( comments_open() || get_comments_number() ) :
			- comments_template();
		- endif
	- endwhile

